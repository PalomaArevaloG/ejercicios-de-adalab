'use strict';
// Obtenemos el elemento con el que queremos trabajar usando document.querySelector()
const password = document.querySelector('.pass');

// Cambiamos el contenido del elemento, indicando que sea igual al actual, más una nueva palabr"a añadida
password.innerHTML = "**";